import SignupTabletSmsScreen from "./SignupTabletSmsScreen";

export const metadata = {
  title: "회원가입(태블릿-문자형)",
};

export default function SignupTabletSmsPage() {
  return <SignupTabletSmsScreen />;
}
